//We get from the user via PROmpt three numbers
const firstNum = Number(prompt("Specify the first number"));
const twoNum = Number(prompt("Specify the second number"));
const thirdNum = Number(prompt("Specify the third number"));
const participants = 3;
//show through Alert arithmetic of these numbers
alert(`${(firstNum + twoNum + thirdNum) / participants}`);
